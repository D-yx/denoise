name: denoise
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pytorch=2.5.1
  - pytorch-cuda=12.4
  - numpy<2.0
  - scipy
  - libsndfile
  - pysoundfile
  - tensorboard
  - tqdm
  - pip
  - pip:
      - pesq
      - pystoi
